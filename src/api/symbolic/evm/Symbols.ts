export enum Symbols {
  // temporal placeholder until full symexec is implemented
  UNKNOWN,
  ADDRESS,
  ORIGIN,
  CALLER,
  BALANCE,
  CALLVALUE,
  CALLDATASIZE,
  CALLDATALOAD,
  CODESIZE,
  GAS
}
